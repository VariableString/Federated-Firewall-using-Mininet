system:
  log_level: INFO
  
mininet:
  num_clients: 3
  
federated:
  rounds: 10
  local_epochs: 3
  learning_rate: 0.01
  batch_size: 16
  
model:
  input_size: 10
  hidden_size: 32
  output_size: 2
  
security:
  threat_threshold: 0.6
